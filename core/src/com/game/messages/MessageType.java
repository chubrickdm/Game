package com.game.messages;

public enum MessageType{
	move, deleteObject, characterChange, levelComplete, returnPosition, destroyObject,
	playerLost, addObject, goTo, comeTo, disconnect, whoseBody, isMyBody, unknown
}
